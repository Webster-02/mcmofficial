function adminPage(){const d=window.portalData||{classes:[],svls:[],students:[]};return `<div class="admin-wrap"><div class="admin-hero"><div><p class="eyebrow">University administration</p><h1>Management Center</h1><p class="muted">Manage academic relationships and portal accounts only.</p></div><button class="admin-btn" onclick="adminAdd()">+ Add Record</button></div><div class="admin-grid"><div class="admin-stat"><span class="muted">Classes</span><strong>${d.classes.length}</strong></div><div class="admin-stat"><span class="muted">SVLs</span><strong>${d.svls.length}</strong></div><div class="admin-stat"><span class="muted">Students</span><strong>${d.students.length}</strong></div><div class="admin-stat"><span class="muted">Access accounts</span><strong>${d.svls.length+d.students.length}</strong></div></div><div class="admin-card"><div class="admin-tabs"><button class="admin-tab active" onclick="adminTab('classes',this)">Classes</button><button class="admin-tab" onclick="adminTab('svls',this)">SVLs</button><button class="admin-tab" onclick="adminTab('students',this)">Students</button></div><div id="admin-table-area">${adminTable('classes')}</div></div></div>`}function adminTable(type){const d=window.portalData[type]||[];if(type==='classes')return `<table class="admin-table"><thead><tr><th>ID</th><th>Class</th><th>Assigned SVL</th><th>Students</th><th>Status</th><th>Action</th></tr></thead><tbody>${d.map(x=>`<tr><td>${x.id}</td><td>${x.name}</td><td>${x.svl}</td><td>${x.students}</td><td>${x.status}</td><td><button class="admin-btn secondary" onclick="adminToast('Class details ready for backend connection')">View</button></td></tr>`).join('')}</tbody></table>`;if(type==='svls')return `<table class="admin-table"><thead><tr><th>ID</th><th>Name</th><th>Email</th><th>Assigned Class</th><th>Action</th></tr></thead><tbody>${d.map(x=>`<tr><td>${x.id}</td><td>${x.name}</td><td>${x.email}</td><td>${x.className}</td><td><button class="admin-btn secondary" onclick="adminToast('SVL record selected')">Manage</button></td></tr>`).join('')}</tbody></table>`;return `<table class="admin-table"><thead><tr><th>ID</th><th>Name</th><th>Email</th><th>Class</th><th>SVL</th><th>Action</th></tr></thead><tbody>${d.map(x=>`<tr><td>${x.id}</td><td>${x.name}</td><td>${x.email}</td><td>${x.className}</td><td>${x.svl}</td><td><button class="admin-btn secondary" onclick="adminToast('Student record selected')">Manage</button></td></tr>`).join('')}</tbody></table>`}function adminTab(type,button){document.querySelectorAll('.admin-tab').forEach(b=>b.classList.remove('active'));button.classList.add('active');document.querySelector('#admin-table-area').innerHTML=adminTable(type)}function adminAdd(){adminToast('Add form will connect to the backend data model next.')}function adminToast(message){alert(message)}function renderAdminRoute(){if(location.hash.replace('#','')==='admin')document.querySelector('#app').innerHTML=adminPage()}window.addEventListener('hashchange',renderAdminRoute);if(location.hash.replace('#','')==='admin')renderAdminRoute();