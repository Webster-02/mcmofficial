(function(){'use strict';
async function renderLiveSVL(){if(location.hash!=='#svl-dashboard')return;const app=document.querySelector('#app');if(!app)return;try{const db=window.mcmDb;if(!db)throw Error('Supabase unavailable');const p=await db.profile();if(!p||p.role!=='svl')return;const classes=await db.classes();let memberships=[];for(const c of classes){const rows=await db.members(c.id);if(rows.some(r=>r.user_id===p.id))memberships.push({c,rows});}const item=memberships[0];const students=item?item.rows.filter(r=>r.member_type==='student'||r.profiles?.role==='student'):[];const notices=item?await db.notices(item.c.id):[];app.innerHTML=`<main class="svl-page"><div class="svl-head"><div><h1>SVL Dashboard</h1><p class="svl-sub">Welcome, ${p.full_name||'SVL'}. Manage your assigned class.</p></div><a class="btn btn-primary" href="#logout">Logout</a></div><section class="svl-grid"><article class="svl-card"><h3>Assigned Class</h3><div class="svl-number">${item?.c?.name||'Not assigned'}</div><p class="svl-sub">${item?.c?.code||'Ask admin for assignment'}</p></article><article class="svl-card"><h3>Students</h3><div class="svl-number">${students.length}</div><p class="svl-sub">Only your assigned students</p></article><article class="svl-card"><h3>Class Notices</h3><div class="svl-number">${notices.length}</div><p class="svl-sub">Live Supabase data</p></article></section><section class="svl-actions"><button class="svl-action" onclick="alert('Slides upload storage will be enabled next.')"><strong>Upload Slides / Notes</strong><span>Add learning material for your class.</span></button><button class="svl-action" onclick="alert('Notice creation form will be enabled next.')"><strong>Give Class Notice</strong><span>Share an important update.</span></button><button class="svl-action" onclick="location.hash='timetable'"><strong>View Timetable</strong><span>Check your assigned class schedule.</span></button><button class="svl-action" onclick="location.hash='discussions'"><strong>Class Community</strong><span>Connect with your students.</span></button><button class="svl-action" onclick="alert('${students.map(s=>s.profiles?.full_name||'Student').join('\\n')||'No students assigned yet.'}')"><strong>View Students</strong><span>See students in ${item?.c?.name||'your class'}.</span></button><button class="svl-action" onclick="location.hash='logout'"><strong>Logout</strong><span>Exit the SVL portal.</span></button></section><div class="svl-notice"><strong>Access limit:</strong> Student transfers, class assignments, and account management belong to University Admin.</div></main>`}catch(e){console.error(e);app.innerHTML='<main class="svl-page"><div class="svl-notice">Unable to load live SVL data. Please check Supabase setup.</div></main>'}}
window.addEventListener('hashchange',renderLiveSVL);renderLiveSVL();})();